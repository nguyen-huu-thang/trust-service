spring:
  application:
    name: key

  datasource:
    url: jdbc:postgresql://localhost:5432/key_service
    username: thang
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # flyway:
  #   enabled: true
  #   locations: classpath:db/migration
  #   baseline-on-migrate: true

logging:
  level:
    org.flywaydb: DEBUG


key:
  encryption:
    secret: QlLW7cokIMS0yMki/JuRk3OvUnt/nBw+qkvCqXbWyg8=

server:
  port: 8081